{
  "buildCommand": "npm install -g pnpm && pnpm install && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/google-reviews-boost run build",
  "outputDirectory": "artifacts/google-reviews-boost/dist/public",
  "installCommand": "echo skip",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
